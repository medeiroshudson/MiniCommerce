{
  "name": "@minicommerce/root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "compile": "turbo run build",
    "start": "turbo run start --filter=./packages/backend"
  },
  "devDependencies": {
    "bun-types": "^1.0.3",
    "dotenv-cli": "^7.3.0",
    "turbo": "latest",
    "@minicommerce/shared": "1.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
