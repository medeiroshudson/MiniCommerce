{
  "name": "@minicommerce/backend",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "compile": "bun build ./src/index.ts --outdir ./dist --target bun",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "hono": "^3.7.1"
  },
  "devDependencies": {
    "@minicommerce/shared": "workspace:*"
  }
}